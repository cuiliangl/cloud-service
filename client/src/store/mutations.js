/**
 * @file mutations
 * @author cuiliang
 * @since 2020.01.14
 */

/**
 * 命名规范：
 * 以const声明常量，大写，必须为全局唯一。
 * 命名规则：
 * 1. view名 + 模块名 + 动作
 * 2. store中的actions也必须遵守这个规则
 */

export const HOME_HELLO_WORLD_GET_USERINFO = 'HOME_HELLO_WORLD_GET_USERINFO'
